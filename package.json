{
	"name": "rate-limited-api",
	"version": "1.0.0",
	"main": "index.js",
	"repository": "git@github.com:MoonTory/rate-limited-api.git",
	"license": "MIT",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"bootstrap": "chmod u+rx ./scripts/start_services.sh",
		"start:dev": "ENV=development ./scripts/start_services.sh",
		"docker:kill": "docker stop $(docker ps -a -q) && docker rm $(docker ps -aq) && docker rmi $(docker images -q)"
	}
}
